description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: '../properties/status.yaml'
        query:
          $ref: '../properties/query_id.yaml'
        feed:
          $ref: '../properties/feed_itunes.yaml'
        description:
          $ref: '../properties/description_response.yaml'
