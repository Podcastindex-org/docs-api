description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: '../properties/status.yaml'
        feeds:
          $ref: '../properties/feeds.yaml'
        count:
          $ref: '../properties/count.yaml'
        query:
          $ref: '../properties/query_search.yaml'
        description:
          $ref: '../properties/description_response.yaml'
