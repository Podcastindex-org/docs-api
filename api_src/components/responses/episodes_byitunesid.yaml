description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: '../properties/status.yaml'
        items:
          $ref: '../properties/items_itunesId.yaml'
        count:
          $ref: '../properties/count.yaml'
        query:
          $ref: '../properties/id_query.yaml'
        description:
          $ref: '../properties/description_response.yaml'
