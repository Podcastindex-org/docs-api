description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: '../properties/status.yaml'
        feeds:
          $ref: '../properties/feeds.yaml'
        count:
          $ref: '../properties/count.yaml'
        medium:
          $ref: '../properties/medium.yaml'
        description:
          $ref: '../properties/description_response.yaml'
