description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: '../properties/status.yaml'
        items:
          $ref: '../properties/items_soundbites.yaml'
        count:
          $ref: '../properties/count.yaml'
        description:
          $ref: '../properties/description_response.yaml'
