description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: '../properties/status.yaml'
        items:
          $ref: '../properties/items_podcast.yaml'
        count:
          $ref: '../properties/count.yaml'
        query:
          $ref: '../properties/query_url.yaml'
        description:
          $ref: '../properties/description_response.yaml'
