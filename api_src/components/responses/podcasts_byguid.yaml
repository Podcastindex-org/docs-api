description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          $ref: '../properties/status.yaml'
        query:
          $ref: '../properties/query_guid.yaml'
        feed:
          $ref: '../properties/feed_podcast.yaml'
        description:
          $ref: '../properties/description_response.yaml'
